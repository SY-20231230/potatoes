import React, {useState} from "react"

const User_history = ({
                          userhistory_id,
                          user_id,
                          userhistory_start,
                          userhistory_end,
                          userhistory_via,
                          userhistory_searchtime
                      }) => {

    return (
        <>
history
        </>
    )
}

export default User_history
